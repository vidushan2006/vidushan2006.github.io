<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIDU - Leveling Up! 🚀</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --glow-1: #7b61ff;
            --glow-2: #39ff14;
            --glow-3: #00c6ff;
            --glow-4: #ff6b6b;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #04060c;
            overflow-x: hidden;
            background-image: 
                radial-gradient(at 27% 37%, hsla(215, 98%, 61%, 0.1) 0px, transparent 50%),
                radial-gradient(at 97% 21%, hsla(125, 98%, 72%, 0.1) 0px, transparent 50%),
                radial-gradient(at 52% 99%, hsla(355, 98%, 76%, 0.1) 0px, transparent 50%),
                radial-gradient(at 10% 29%, hsla(256, 96%, 68%, 0.1) 0px, transparent 50%),
                radial-gradient(at 97% 96%, hsla(38, 60%, 74%, 0.1) 0px, transparent 50%),
                radial-gradient(at 33% 50%, hsla(222, 67%, 73%, 0.1) 0px, transparent 50%),
                radial-gradient(at 79% 53%, hsla(343, 68%, 79%, 0.1) 0px, transparent 50%);
        }

        .animated-gradient-bg {
            width: 100%;
            height: 100%;
            position: fixed;
            top: 0;
            left: 0;
            z-index: -1;
            background: linear-gradient(-45deg, #04060c, #0a0c1a, #04060c, #101220);
            background-size: 400% 400%;
            animation: gradient-animation 15s ease infinite;
        }

        @keyframes gradient-animation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        
        .glass-card {
            background: rgba(20, 22, 28, 0.5);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 16px 50px rgba(2, 4, 12, 0.6);
        }
        
        .countdown-box {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.06);
        }
        
        .title-glow {
            text-shadow: 0 0 10px var(--glow-2), 0 0 20px var(--glow-1), 0 0 40px var(--glow-3);
        }

        .feature-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .feature-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 24px 60px rgba(2, 4, 12, 0.8);
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb {
            background: rgba(57, 255, 20, 0.5);
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: rgba(57, 255, 20, 0.8);
        }

    </style>
</head>
<body class="text-gray-200">
    <div class="animated-gradient-bg"></div>

    <main class="min-h-screen w-full flex flex-col items-center justify-center p-4 sm:p-6 lg:p-8">
        <div class="w-full max-w-5xl mx-auto">
            
            <!-- Main Maintenance Card -->
            <div id="main-card" class="glass-card rounded-2xl p-6 sm:p-10 text-center animate-fade-in-up">
                <div class="inline-block bg-green-500/10 text-green-400 text-sm font-bold px-4 py-1.5 rounded-full mb-4">
                    STATUS: LEVELING UP 🚀
                </div>
                <h1 class="text-4xl md:text-6xl font-black text-white title-glow">We're getting a glow up.</h1>
                <p class="mt-4 max-w-2xl mx-auto text-lg md:text-xl text-gray-400">
                    Ayo, the site is down for a sec while we push a massive update. It's gonna be bussin', fr fr. 💅 We'll be back online before you can say "bet."
                </p>

                <!-- Countdown Timer -->
                <div id="countdown" class="mt-8 flex justify-center items-center gap-2 sm:gap-4">
                    <div class="countdown-box p-4 rounded-lg w-24">
                        <span id="days" class="text-4xl font-bold text-white tabular-nums">00</span>
                        <span class="mt-1 block text-xs uppercase text-gray-400 tracking-wider">Days</span>
                    </div>
                    <div class="countdown-box p-4 rounded-lg w-24">
                        <span id="hours" class="text-4xl font-bold text-white tabular-nums">00</span>
                        <span class="mt-1 block text-xs uppercase text-gray-400 tracking-wider">Hours</span>
                    </div>
                    <div class="countdown-box p-4 rounded-lg w-24">
                        <span id="minutes" class="text-4xl font-bold text-white tabular-nums">00</span>
                        <span class="mt-1 block text-xs uppercase text-gray-400 tracking-wider">Minutes</span>
                    </div>
                    <div class="countdown-box p-4 rounded-lg w-24">
                        <span id="seconds" class="text-4xl font-bold text-white tabular-nums">00</span>
                        <span class="mt-1 block text-xs uppercase text-gray-400 tracking-wider">Seconds</span>
                    </div>
                </div>
            </div>

            <!-- Feature Recap Section -->
            <div id="features-card" class="mt-8 glass-card rounded-2xl p-6 sm:p-10 animate-fade-in-up" style="animation-delay: 200ms;">
                <h2 class="text-2xl md:text-3xl font-bold text-white text-center">In case you forgot what you're missin'... 👀</h2>
                <p class="text-center mt-2 text-gray-400">A full breakdown of all the fire features VIDU cooked up:</p>
                
                <div class="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <!-- The Player -->
                    <div class="feature-card glass-card rounded-xl p-6">
                        <h3 class="text-xl font-bold text-white">🎬 The VIDU Player™️</h3>
                        <p class="text-sm text-gray-400 mt-1">Yeah, I built that. The default player is mid.</p>
                        <ul class="mt-4 space-y-2 text-sm list-disc list-inside text-gray-300">
                            <li><strong>Custom Controls:</strong> A whole custom UI, 'cause we're not basic.</li>
                            <li><strong>Keyboard Warrior Mode:</strong> Full hotkey support so you can feel like a hacker.</li>
                            <li><strong>Speed Demon Settings:</strong> Bend time with playback speed controls.</li>
                            <li><strong>"Never Lose Your Spot" Save:</strong> Your browser's secret diary (`localStorage`) remembers your timestamp. Magic. ✨</li>
                            <li><strong>Pop-out Player:</strong> Multitask like a pro.</li>
                            <li><strong>Invert Colors:</strong> For the late-night grind sessions.</li>
                            <li><strong>In-Player Playlist:</strong> Navigate without ever leaving the vibe.</li>
                            <li><strong>Mobile Gestures:</strong> Double-tap & swipe to seek. It just works.</li>
                            <li><strong>Rainbow Progress Bar:</strong> It's a whole vibe when you press play. 🌈</li>
                        </ul>
                    </div>

                    <!-- Security & Playlists -->
                    <div class="feature-card glass-card rounded-xl p-6">
                        <h3 class="text-xl font-bold text-white">🔐 The Vault & Playlists</h3>
                        <p class="text-sm text-gray-400 mt-1">High-level security, you feel me?</p>
                        <ul class="mt-4 space-y-2 text-sm list-disc list-inside text-gray-300">
                            <li><strong>Password Protection:</strong> For the exclusive content. 🤫 (iykyk)</li>
                            <li><strong>Multi-User Login:</strong> VIP treatment only. No randos.</li>
                            <li><strong>YT Playlist Importer:</strong> Drop a link and my API magic does the rest.</li>
                            <li><strong>Fullscreen Browser:</strong> An immersive experience for finding your next binge.</li>
                        </ul>
                    </div>

                    <!-- The Tools -->
                    <div class="feature-card glass-card rounded-xl p-6">
                        <h3 class="text-xl font-bold text-white">🛠️ The VIDU Tool Shed</h3>
                        <p class="text-sm text-gray-400 mt-1">Utilities that actually slap.</p>
                        <ul class="mt-4 space-y-2 text-sm list-disc list-inside text-gray-300">
                            <li><strong>The Video Yoinker:</strong> Grab videos for offline viewing. You're welcome.</li>
                            <li><strong>Link Glo-Up:</strong> Turns ugly YouTube links into clean `youtu.be` links. Aesthetic.</li>
                            <li><strong>Downloads Panel:</strong> Easy access to the desktop & Android apps.</li>
                        </ul>
                    </div>

                    <!-- The Vibe (UI/UX) -->
                    <div class="feature-card glass-card rounded-xl p-6 lg:col-span-3">
                         <h3 class="text-xl font-bold text-white">✨ The Vibe (UI/UX)</h3>
                        <p class="text-sm text-gray-400 mt-1">Coded the soul into this site.</p>
                        <ul class="mt-4 space-y-2 text-sm list-disc list-inside text-gray-300 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-x-6 gap-y-2">
                            <li>Glassy Everything (`backdrop-filter`)</li>
                            <li>Buttery Smooth Animations</li>
                            <li>Click Ripple Effects</li>
                            <li>Fully Responsive (looks good on your phone, duh)</li>
                            <li>Notifications Panel</li>
                            <li>Site Analytics (we see you lurkin' 👀)</li>
                            <li>No Right-Click (nice try)</li>
                        </ul>
                    </div>
                </div>
            </div>
            
            <!-- Footer -->
            <footer class="text-center mt-10 pb-4">
                <p class="text-gray-500 text-sm">Coded with 💀 and ☕ by <span class="font-bold text-gray-400">VIDU</span></p>
            </footer>

        </div>
    </main>

    <script>
        // Set the date we're counting down to
        const countDownDate = new Date("Oct 18, 2025 15:30:00").getTime();

        const countdownFunction = setInterval(function() {
            const now = new Date().getTime();
            const distance = countDownDate - now;

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById("days").innerText = days.toString().padStart(2, '0');
            document.getElementById("hours").innerText = hours.toString().padStart(2, '0');
            document.getElementById("minutes").innerText = minutes.toString().padStart(2, '0');
            document.getElementById("seconds").innerText = seconds.toString().padStart(2, '0');

            if (distance < 0) {
                clearInterval(countdownFunction);
                document.getElementById("countdown").innerHTML = "<div class='text-2xl font-bold text-green-400'>We should be back now! Try refreshing.</div>";
            }
        }, 1000);

        // Simple fade-in animation using JS
        document.addEventListener("DOMContentLoaded", () => {
            const cards = document.querySelectorAll('.animate-fade-in-up');
            cards.forEach(card => {
                card.classList.add('opacity-0', 'transform', 'translate-y-4');
                setTimeout(() => {
                    card.classList.remove('opacity-0', 'translate-y-4');
                    card.classList.add('opacity-100', 'translate-y-0', 'transition-all', 'duration-700', 'ease-out');
                }, 100); // Small delay to ensure initial styles apply
            });
        });
    </script>

</body>
</html>
